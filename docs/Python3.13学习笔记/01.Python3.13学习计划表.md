---
title: Python3.13学习计划表
date: 2025-12-25 10:15:03
permalink: /pages/090798/
categories:
  - 13学习笔记
tags:
  -
author:
  name: MeiChen
  link: https://github.com/mtl-123
---
# Python 3.13 零基础详细学习计划表

**学习前提**：无需编程基础，每天投入1-2小时，共6周时间

## 📅 第1周：Python基础入门

### **Day 1-2：环境搭建与第一个程序**

**学习内容**：

- 安装Python 3.13（最新版本于2024年10月7日发布）
- 了解新REPL交互式解释器的多行编辑功能
- 编写第一个Hello World程序

**案例讲解**：

```python
# 第一个Python程序
print("Hello, World!")
print("欢迎来到Python 3.13的世界！")

# 在新REPL中可以多行编辑：
# >>> print("Hello")
# ... print("World")
# Hello
# World
```

### **Day 3-4：变量与数据类型**

**学习内容**：

- 变量命名规则
- 基本数据类型：整数(int)、浮点数(float)、字符串(str)、布尔值(bool)

**案例讲解**：

```python
# 变量定义
age = 25              # 整数
height = 1.75         # 浮点数
name = "张三"         # 字符串
is_student = True     # 布尔值

print(f"姓名: {name}, 年龄: {age}, 身高: {height}m, 学生: {is_student}")
# 输出: 姓名: 张三, 年龄: 25, 身高: 1.75m, 学生: True
```

### **Day 5-7：基础运算符（重点！）**

**学习内容**：

- **算术运算符**：`+`, `-`, `*`, `/`, `//`, `%`, `**`
- **赋值运算符**：`=`, `+=`, `-=`, `*=`, `/=`
- **比较运算符**：`==`, `!=`, `>`, `<`, `>=`, `<=`

**案例讲解**：

```python
# 算术运算符
a = 10
b = 3
print(f"{a} + {b} = {a + b}")    # 13
print(f"{a} / {b} = {a / b}")    # 3.333...
print(f"{a} // {b} = {a // b}")  # 3 (整除)
print(f"{a} % {b} = {a % b}")    # 1 (取余)
print(f"{a} ** {b} = {a ** b}")  # 1000 (幂运算)

# 赋值运算符
x = 5
x += 3    # 等价于 x = x + 3
print(f"x = {x}")  # 8

# 比较运算符
age = 18
print(f"年龄大于等于18岁吗? {age >= 18}")  # True
print(f"年龄等于20岁吗? {age == 20}")      # False
```

---

## 📅 第2周：流程控制

### **Day 8-9：条件语句**

**学习内容**：

- `if`, `elif`, `else` 语句
- 逻辑运算符：`and`, `or`, `not`

**案例讲解**：

```python
# 成绩评级系统
score = 85

if score >= 90:
    grade = "A"
elif score >= 80:
    grade = "B"
elif score >= 70:
    grade = "C"
else:
    grade = "D"

print(f"成绩: {score}, 等级: {grade}")  # B

# 逻辑运算符
has_license = True
age = 20
can_drive = has_license and age >= 18
print(f"可以开车吗? {can_drive}")  # True
```

### **Day 10-12：循环语句**

**学习内容**：

- `for` 循环
- `while` 循环
- `break`, `continue` 语句

**案例讲解**：

```python
# for循环 - 打印乘法表
print("乘法表:")
for i in range(1, 6):  # 1到5
    for j in range(1, i + 1):
        print(f"{j}×{i}={i*j}", end="\t")
    print()  # 换行

# while循环 - 猜数字游戏
import random
secret_number = random.randint(1, 10)
guesses = 0

print("\n猜数字游戏(1-10):")
while guesses < 3:
    guess = int(input("猜一个数字: "))
    guesses += 1

    if guess == secret_number:
        print("恭喜你猜对了!")
        break
    elif guess < secret_number:
        print("太小了!")
    else:
        print("太大了!")
else:
    print(f"游戏结束! 正确答案是: {secret_number}")
```

---

## 📅 第3周：数据结构

### **Day 13-14：列表(List)**

**学习内容**：

- 列表的创建、访问、修改
- 列表常用方法

**案例讲解**：

```python
# 购物清单管理
shopping_list = ["苹果", "香蕉", "面包", "牛奶"]

print("初始购物清单:", shopping_list)
# 添加商品
shopping_list.append("鸡蛋")
print("添加鸡蛋后:", shopping_list)

# 删除商品
shopping_list.remove("香蕉")
print("删除香蕉后:", shopping_list)

# 修改商品
shopping_list[0] = "红富士苹果"
print("修改苹果后:", shopping_list)

# 遍历列表
print("\n购物清单详情:")
for index, item in enumerate(shopping_list, 1):
    print(f"{index}. {item}")
```

### **Day 15-16：字典(Dictionary)**

**学习内容**：

- 字典的创建、访问、修改
- 字典常用方法

**案例讲解**：

```python
# 学生信息管理系统
student = {
    "name": "李四",
    "age": 20,
    "major": "计算机科学",
    "grades": {
        "math": 85,
        "english": 92,
        "python": 95
    }
}

# 访问信息
print(f"学生姓名: {student['name']}")
print(f"专业: {student['major']}")

# 计算平均成绩
grades = student["grades"].values()
average_grade = sum(grades) / len(grades)
print(f"平均成绩: {average_grade:.1f}")

# 添加新课程
student["grades"]["database"] = 88
print("添加数据库课程后:", student["grades"])

# 遍历字典
print("\n所有课程成绩:")
for course, score in student["grades"].items():
    print(f"{course}: {score}分")
```

### **Day 17：集合(Set)和元组(Tuple)**

**学习内容**：

- 集合的特性与操作
- 元组的不可变性

**案例讲解**：

```python
# 集合 - 去重和集合运算
# 学生选课情况
course_A = {"张三", "李四", "王五", "赵六"}
course_B = {"李四", "王五", "钱七", "孙八"}

print("课程A学生:", course_A)
print("课程B学生:", course_B)

# 交集 - 同时选两门课的学生
common_students = course_A & course_B
print("同时选两门课的学生:", common_students)

# 并集 - 所有选课学生
all_students = course_A | course_B
print("所有选课学生:", all_students)

# 元组 - 不可变的数据
coordinates = (10, 20)  # 坐标点
rgb_color = (255, 0, 0)  # 红色

print(f"坐标点: ({coordinates[0]}, {coordinates[1]})")
print(f"RGB颜色: {rgb_color}")

# 元组解包
x, y = coordinates
print(f"解包后: x={x}, y={y}")
```

---

## 📅 第4周：函数与模块

### **Day 18-19：函数基础**

**学习内容**：

- 函数的定义与调用
- 参数传递
- 返回值

**案例讲解**：

```python
# 计算器函数
def add(a, b):
    """加法函数"""
    return a + b

def subtract(a, b):
    """减法函数"""
    return a - b

def multiply(a, b):
    """乘法函数"""
    return a * b

def divide(a, b):
    """除法函数，处理除零错误"""
    if b == 0:
        return "错误：除数不能为0"
    return a / b

# 使用函数
num1 = float(input("输入第一个数字: "))
num2 = float(input("输入第二个数字: "))

print(f"{num1} + {num2} = {add(num1, num2)}")
print(f"{num1} - {num2} = {subtract(num1, num2)}")
print(f"{num1} × {num2} = {multiply(num1, num2)}")
print(f"{num1} ÷ {num2} = {divide(num1, num2)}")
```

### **Day 20-21：模块与标准库**

**学习内容**：

- 导入模块
- 常用标准库：`math`, `random`, `datetime`

**案例讲解**：

```python
import math
import random
from datetime import datetime, timedelta

# 数学计算
print("=== 数学计算 ===")
radius = 5
area = math.pi * radius ** 2
print(f"半径为{radius}的圆面积: {area:.2f}")

# 随机数生成
print("\n=== 随机数生成 ===")
random_number = random.randint(1, 100)
print(f"随机整数(1-100): {random_number}")

random_float = random.random()  # 0-1之间的浮点数
print(f"随机浮点数: {random_float:.4f}")

# 日期时间处理
print("\n=== 日期时间处理 ===")
now = datetime.now()
print(f"当前时间: {now.strftime('%Y-%m-%d %H:%M:%S')}")

# 计算10天后的日期
future_date = now + timedelta(days=10)
print(f"10天后: {future_date.strftime('%Y-%m-%d')}")

# 日期差计算
birthday = datetime(2000, 1, 1)
age_days = (now - birthday).days
age_years = age_days / 365.25
print(f"从2000年1月1日到现在: {age_days}天，约{age_years:.1f}年")
```

---

## 📅 第5周：文件操作与异常处理

### **Day 22-23：文件读写**

**学习内容**：

- 文件打开与关闭
- 读取文件内容
- 写入文件内容

**案例讲解**：

```python
# 日记本程序
def write_diary():
    """写日记功能"""
    print("\n=== 写日记 ===")
    date = datetime.now().strftime("%Y-%m-%d")
    content = input("请输入日记内容: ")

    with open("diary.txt", "a", encoding="utf-8") as file:
        file.write(f"{date}: {content}\n")
    print("日记保存成功!")

def read_diary():
    """读日记功能"""
    print("\n=== 读日记 ===")
    try:
        with open("diary.txt", "r", encoding="utf-8") as file:
            entries = file.readlines()

        if not entries:
            print("日记本为空")
            return

        print("所有日记条目:")
        for i, entry in enumerate(entries, 1):
            print(f"{i}. {entry.strip()}")

    except FileNotFoundError:
        print("日记文件不存在，请先写日记")

# 主程序
while True:
    print("\n=== 个人日记本 ===")
    print("1. 写日记")
    print("2. 读日记")
    print("3. 退出")

    choice = input("请选择功能(1-3): ")

    if choice == "1":
        write_diary()
    elif choice == "2":
        read_diary()
    elif choice == "3":
        print("感谢使用日记本，再见!")
        break
    else:
        print("无效选择，请重新输入")
```

### **Day 24：异常处理**

**学习内容**：

- `try`, `except`, `finally` 语句
- 常见异常类型

**案例讲解**：

```python
# 银行账户系统
class BankAccount:
    def __init__(self, account_number, balance=0):
        self.account_number = account_number
        self.balance = balance

    def deposit(self, amount):
        """存款"""
        if amount <= 0:
            raise ValueError("存款金额必须大于0")
        self.balance += amount
        return self.balance

    def withdraw(self, amount):
        """取款"""
        if amount <= 0:
            raise ValueError("取款金额必须大于0")
        if amount > self.balance:
            raise ValueError("余额不足")
        self.balance -= amount
        return self.balance

# 使用异常处理
def main():
    account = BankAccount("123456", 1000)
    print(f"初始余额: {account.balance}元")

    while True:
        print("\n=== 银行系统 ===")
        print("1. 存款")
        print("2. 取款")
        print("3. 查询余额")
        print("4. 退出")

        try:
            choice = input("请选择功能(1-4): ")

            if choice == "1":
                amount = float(input("请输入存款金额: "))
                new_balance = account.deposit(amount)
                print(f"存款成功! 新余额: {new_balance}元")

            elif choice == "2":
                amount = float(input("请输入取款金额: "))
                new_balance = account.withdraw(amount)
                print(f"取款成功! 新余额: {new_balance}元")

            elif choice == "3":
                print(f"当前余额: {account.balance}元")

            elif choice == "4":
                print("感谢使用银行系统，再见!")
                break

            else:
                print("无效选择，请重新输入")

        except ValueError as e:
            print(f"错误: {e}")
        except Exception as e:
            print(f"系统错误: {e}")

if __name__ == "__main__":
    main()
```

---

## 📅 第6周：面向对象编程与项目实战

### **Day 25-26：面向对象基础**

**学习内容**：

- 类与对象
- 属性与方法
- 构造函数

**案例讲解**：

```python
# 图书管理系统
class Book:
    def __init__(self, title, author, isbn, available=True):
        self.title = title
        self.author = author
        self.isbn = isbn
        self.available = available

    def __str__(self):
        status = "可借" if self.available else "已借出"
        return f"《{self.title}》- {self.author} ({self.isbn}) [{status}]"

    def borrow(self):
        if not self.available:
            raise Exception("该书已被借出")
        self.available = False
        return True

    def return_book(self):
        if self.available:
            raise Exception("该书已在馆内")
        self.available = True
        return True

class Library:
    def __init__(self):
        self.books = []

    def add_book(self, book):
        self.books.append(book)
        print(f"添加图书: {book.title}")

    def find_book(self, keyword):
        results = []
        for book in self.books:
            if keyword.lower() in book.title.lower() or keyword.lower() in book.author.lower():
                results.append(book)
        return results

    def display_all_books(self):
        if not self.books:
            print("图书馆暂无图书")
            return

        print("\n=== 图书馆所有图书 ===")
        for i, book in enumerate(self.books, 1):
            print(f"{i}. {book}")

# 使用示例
library = Library()

# 添加图书
book1 = Book("Python编程从入门到实践", "Eric Matthes", "978-7-111-60755-1")
book2 = Book("算法导论", "Thomas H. Cormen", "978-7-111-40701-0")
book3 = Book("深入理解计算机系统", "Randal E. Bryant", "978-7-111-44348-8")

library.add_book(book1)
library.add_book(book2)
library.add_book(book3)

# 显示所有图书
library.display_all_books()

# 搜索图书
search_results = library.find_book("Python")
print("\n=== 搜索结果 ===")
for book in search_results:
    print(book)

# 借书
print("\n=== 借书操作 ===")
try:
    book1.borrow()
    print(f"成功借出: {book1.title}")
    library.display_all_books()
except Exception as e:
    print(f"借书失败: {e}")
```

### **Day 27-30：综合项目实战**

**项目：天气查询系统**

```python
import requests
import json
from datetime import datetime

class WeatherApp:
    def __init__(self):
        self.api_key = "YOUR_API_KEY"  # 需要注册免费API
        self.base_url = "http://api.weatherapi.com/v1"

    def get_weather(self, city):
        """获取天气数据"""
        try:
            url = f"{self.base_url}/current.json"
            params = {
                'key': self.api_key,
                'q': city,
                'lang': 'zh'  # 中文
            }

            response = requests.get(url, params=params)
            response.raise_for_status()  # 检查HTTP错误

            data = response.json()
            return self._parse_weather_data(data)

        except requests.exceptions.RequestException as e:
            print(f"网络请求错误: {e}")
            return None
        except KeyError as e:
            print(f"数据解析错误: {e}")
            return None

    def _parse_weather_data(self, data):
        """解析天气数据"""
        current = data['current']
        location = data['location']

        weather_info = {
            'city': location['name'],
            'country': location['country'],
            'temp_c': current['temp_c'],
            'temp_f': current['temp_f'],
            'condition': current['condition']['text'],
            'humidity': current['humidity'],
            'wind_kph': current['wind_kph'],
            'wind_dir': current['wind_dir'],
            'last_updated': current['last_updated']
        }
        return weather_info

    def display_weather(self, weather_info):
        """显示天气信息"""
        if not weather_info:
            print("无法获取天气信息")
            return

        print(f"\n=== {weather_info['city']}, {weather_info['country']} 天气 ===")
        print(f"温度: {weather_info['temp_c']}°C ({weather_info['temp_f']}°F)")
        print(f"天气状况: {weather_info['condition']}")
        print(f"湿度: {weather_info['humidity']}%")
        print(f"风速: {weather_info['wind_kph']} km/h, 风向: {weather_info['wind_dir']}")
        print(f"更新时间: {weather_info['last_updated']}")

    def run(self):
        """运行天气应用"""
        print("=== 天气查询系统 ===")
        print("输入城市名称查询天气，输入'exit'退出")

        while True:
            city = input("\n请输入城市名称: ").strip()

            if city.lower() == 'exit':
                print("感谢使用天气查询系统，再见!")
                break

            if not city:
                print("请输入有效的城市名称")
                continue

            print(f"正在查询 {city} 的天气...")
            weather_info = self.get_weather(city)

            if weather_info:
                self.display_weather(weather_info)
            else:
                print(f"无法获取 {city} 的天气信息，请检查城市名称是否正确")

# 注意：实际使用需要注册免费的WeatherAPI
# 替换 YOUR_API_KEY 为你的API密钥
if __name__ == "__main__":
    app = WeatherApp()
    app.run()
```

## 🎯 学习建议

1. **循序渐进**：严格按照计划表学习，不要跳过基础内容
2. **动手实践**：每个案例都要亲自敲代码，不要复制粘贴
3. **理解优先**：先理解概念，再记忆语法
4. **调试能力**：学会使用Python 3.13改进的错误消息来调试代码
5. **项目驱动**：学完每个阶段后，尝试修改案例或创建小项目

## 🚀 Python 3.13 新特性利用

- **新REPL**：利用多行编辑功能来测试代码片段
- **改进的错误消息**：当代码出错时，Python 3.13会提供更清晰的错误提示
- **虚拟环境改进**：项目开发时使用虚拟环境隔离依赖

**记住**：编程是实践性很强的技能，每天坚持写代码比一次性学习大量理论更重要！遇到问题时，善用Python 3.13的新特性来帮助你更好地理解和调试代码。
